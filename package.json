{
  "name": "atproto-demo-startupapi-org",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/sync-assets.mjs",
    "dev": "wrangler dev",
    "preview": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@startup-api/cloudflare": "^0.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.60.0"
  }
}
